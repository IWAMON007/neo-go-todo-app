# 2026-05-15

## Claude とのやりとり

### TASKS.md・学習ログ環境のセットアップ

**やったこと:**
- `docs/TASKS.md` を作成し、今後のタスクを整理
- `CLAUDE.md` にタスク管理・メンタリング方針を追記
- `docs/learning/` 以下に daily / inbox / knowledge-vault の構成を構築

**重要なポイント:**
- daily は書き捨て・量優先。knowledge-vault で整理するという役割分担を明確にした
- git の空ディレクトリを追跡するために `.gitkeep` を使う

**Claude からの FB:**
- 学習環境のセットアップに対して「どう記録・整理するか」を自分で考えて方針を決めており、主体的な姿勢が良い
- `daily → inbox → knowledge-vault` のフローを自分で設計できていた点は実務でも活きる視点
- 今後、実装フェーズに入ったときに「なぜこの設計にしたか」を言語化できるかが成長のポイントになりそう

---

### フロントエンドリファクタリングの調査

**やったこと:**
- Claude がコードを調査し、6つの改善ポイントを洗い出した

**重要なポイント:**
- `useTask.ts` の `fetch` + `try/catch` パターンが全関数で重複 → `apiFetch` ヘルパーに共通化できる
- ボタンコンポーネント4つに `.icon-btn` スタイルが重複 → `BaseIconButton` で共通化できる
- `Done.vue` は `getTodoList()` を呼んでいない → `/done` に直接アクセスすると空になるバグ
- `catch (error: any)` → `unknown` に変えるべき（TypeScript の型安全性）
- typo: `CanselButton`（Cancel が正しい）

**Claude からの FB:**
- コードは動いているが「重複」と「型安全性」の2点が今の課題
- `pathName` の文字列比較による分岐は壊れやすい設計で、これを自分で気づいて直せるかが今後の腕の見せどころ

## 自分のメモ

<!-- 気づき・調べたこと・備考 -->

## タスクレビュー（タスク完了時）

### 良かった点

### 改善点
