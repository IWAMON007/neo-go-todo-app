# 2026-06-27

## Claude とのやりとり

### 1-3. TaskTable の責務分離（`refactor/task-table-split`）

**設計の判断**
- `pathName` による文字列分岐 → コンポーネント分割（`TaskTable` + `DoneTaskTable`）に方針決定
- 判断軸：Home と Done でカラム数・表示内容が異なり、Home 側の処理が複雑なため分割が素直
- ルーティングで制御する設計に変えることで、ハードコーディングの排除と柔軟性向上を両立

**`BaseTable.vue` の設計**
- `<slot>` + `:slotted()` を使ってテーブルのスタイルを共通化
- scoped CSS の制約上、slot 内の要素には通常の scoped セレクタが効かないため `:slotted()` を使う必要がある

**コードレビューで指摘された点**
- `Home.vue` に残っていた不要な `useRoute` / `route` 変数・`:pathName` prop を除去
- `TaskTable.vue` の重複 CSS セレクタ（`.edit-cell, .edit-cell` など）を修正
- `DoneTaskTable.vue` の `const props =` を不要な代入として除去

**スコープ外タスクの扱い**
- 作業中に発見したスコープ外の問題（1-6 の typo など）は、指示があるまで触れずに TASKS.md に残す方針を CLAUDE.md に追記

## 自分のメモ

<!-- 気づき・調べたこと・備考 -->

## タスクレビュー（タスク完了時）

### 良かった点

- **設計の判断軸が明確だった** — 「Home と Done でカラム数・処理の複雑さが違う」という具体的な根拠を持ってコンポーネント分割を選択できた。「分割できるから分割する」ではなく、理由が説明できていた点が良い
- **ハードコーディングの問題を正確に言語化できた** — 文字列比較による分岐が「パス名変更時に if 文の修正が必要になる」という実害まで説明できていた
- **ルーティングで制御するという発想** — `pathName` を渡す迂回をやめ、ルーティングの構造に任せるという設計の本質を自分で導き出せた

### 改善点

- **レビュー指摘を一度で全部対応できると◎** — 重複 CSS セレクタや `const props =` の不要な代入は、自分でコードを書く段階で気づけるとより良い。「変数に代入しているのに使っていない」はエディタの警告でも拾えるので意識してみると良い
- **`CanselButton` の typo は書いた本人が気づきたい** — typo はコードレビューで見つかるより、書いた直後に気づくのが理想。補完や静的解析の警告を活用する習慣をつけると防ぎやすい
